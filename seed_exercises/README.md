# Seed configs (8×) — ready-to-use JSON

Deze folder bevat volledig ingevulde `ex01.json` … `ex08.json` met:
- realistische startstructuren
- goal checks
- tip-stappen

## Extra tip-targets gebruikt
- `folderPickerConfirm` — bevestigknop in de verplaatsen-modal (UI code moet dit target exposen)

Als je strikt enkel targets uit CONFIG_SCHEMA wil: voeg in de UI een alias toe, bv. map `folderPickerConfirm` → een bestaand selector-key.
