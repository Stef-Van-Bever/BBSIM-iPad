# Build instructions

1. Zorg dat Node.js beschikbaar is.
2. Run `node tools/build.js`.
3. De SCORM ZIPs verschijnen in `dist/`.

Optioneel: open `dist/ex01/index.html` in een lokale server of in een browser om te testen.
